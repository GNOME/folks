EXTRA_DIST = \
	with-session-bus.sh \
	dbus-session.sh
	manager-file.py \
	$(NULL)

if ENABLE_TRACKER
EXTRA_DIST += with-session-bus-tracker.sh
EXTRA_DIST += tracker.sh
endif

-include $(top_srcdir)/git.mk
